version: "3.8"
services:
  sql-server:
    image: mcr.microsoft.com/azure-sql-edge:latest
    container_name: sql_universe_learning
    user: root
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "YourStrong!Password123"
    ports:
      - "1433:1433"
    volumes:
      - sql_data:/var/opt/mssql/data
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "/opt/mssql-tools/bin/sqlcmd -S localhost -U sa -P 'YourStrong!Password123' -Q 'SELECT 1' || exit 1"]
      interval: 10s
      retries: 10
      start_period: 10s

  sql-setup:
    image: mcr.microsoft.com/azure-sql-edge:latest
    container_name: sql_universe_setup
    depends_on:
      sql-server:
        condition: service_healthy
    volumes:
      - ./setup:/setup
    command: >
      /bin/bash -c "
      echo 'Waiting for SQL Server to be ready...' &&
      /opt/mssql-tools/bin/sqlcmd -S sql-server -U sa -P 'YourStrong!Password123' -d master -i /setup/01_schema.sql -i /setup/02_data.sql -i /setup/03_engine.sql -i /setup/04_solutions_mod01.sql -i /setup/05_solutions_mod02.sql -i /setup/06_solutions_mod03.sql -i /setup/07_solutions_mod04.sql &&
      echo 'Database Setup Completed Successfully! You can now stop this container if you wish.'
      "

volumes:
  sql_data:
